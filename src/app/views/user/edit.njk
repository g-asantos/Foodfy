<!DOCTYPE html>
<html>

    <head>
        <title>Foodfy</title>
         {%include 'parts/styles.njk'%}

    </head>

    <body>
        {%include 'parts/header.njk'%}

        <section>
            <div class="userUpdateContainer">
                <h1>Atualizar Usuário</h1>
                <button class='cancel' type="submit" form='delete' style='background: #DC4747;'>Deletar</button>
                <button class='save' type="submit" form='creation'>Salvar</button>
            </div>
            {%include 'parts/messages.njk'%}
            <form id='creation' class='userEdit' method='POST' action="/users/admin/users?_method=PUT">

                {% include 'user/layout.njk'%}

            </div>
            <input type="hidden" name="id" value={{user.id}}>
            <input type="hidden" name="isAdmin" value={{user.is_admin}}>
            </form>

        <form id='delete' method="POST" action='/users/admin/users/{{user.id}}?_method=DELETE'>

            <input type="hidden" name="id" value={{user.id}}>
        </form>
        
    </body>

</html>