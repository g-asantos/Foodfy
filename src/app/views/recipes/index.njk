<!DOCTYPE html>
<html>

    <head>
        <title>Foodfy</title>
        {%include 'parts/styles.njk'%}

    </head>

    <body>
        {%include 'parts/headerMain.njk'%}
        <section>
            <div class='container'>
                <strong>As melhores receitas</strong>
                <p>Aprenda a construir os melhores pratos com receitas criadas por profissinais do mundo inteiro.</p>
                <img src="\chef.png" alt="Chef"/>
            </div>

            <strong class='topRecipes'>Mais acessadas</strong>
            <div class="recipeContainerFrontPage">

                {% for recipe in recipes %}

                    <div class='card' id='{{recipe.id}}'>

                        <div class='card__image-container'>
                            {%for file in files%}
                                {%for i in range(0, 1)%}
                                    {%if recipe.id == file[i].recipe_id%}
                                        <img src="{{file[0].path}}" alt="{{recipe.title}}"/>
                                        {%endif%}
                                        {%endfor%}
                                        {%endfor%}
                                    </div>

                                    <div class='card__content'>
                                        <p>{{recipe.title}}</p>
                                    </div>
                                    <div class='card__info'>
                                        <p>por
                                            {{recipe.chef_name}}</p>
                                    </div>
                                </div>

                                {%endfor%}
                            </div>
                            
                        </section>

                        
                        {%include 'parts/footer.njk'%}
                        <script src="/scripts.js"></script>
                    </body>

                </html>