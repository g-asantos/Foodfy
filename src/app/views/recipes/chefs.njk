<!DOCTYPE html>
<html>

    <body>

        <head>
            <title>Foodfy</title>
            <link rel="stylesheet" href="/styles/styles.css">
        </head>
        {% include 'parts/headerMain.njk'%}

        <div class="userchefContainer">
            {%for chef in chefs%}
                <div class='card' id='{{chef.id}}'>
                    <div class='card__image-container_chef'>
                        {%for file in files%}
                            {%for i in range(0, 1)%}
                            {%if chef.file_id == file[i].id%}
                                
                        <img src="http://localhost:5000/images/{{file[0].name}}" alt="{{chef.name}}"/>
                            {%endif%}
                                
                            {%endfor%}
                        {%endfor%}
                    </div>
                    <div class='card__content_chef'>
                        <p>{{chef.name}}</p>
                       <a href="chefs/{{chef.id}}" class="see">Visualizar</a>
                    
                    </div>
                    
                </div>
            {% endfor %}
              
        </div>
        <footer id='second'>
            <p>Todos os direitos reservados, receitas Foodfy.</p>
        </footer>
    </div>

    <script src="/scripts.js"></script>
</body>

</html>